1. Shell script란 무엇인가?
    Shell script는 여러 개의 셸 명령어를 파일로 저장해서 한번에 실행 할 수 있도록 만드는것이다.
    장점으로는 배우기 쉽고, 시스템 명령과 궁합이 좋으며, 자동화에 매우 좋다
    단점으로는 복잡한 로직에는 부적합하며, 대규모 프로젝트에는 한계가 보이며, 디버깅이 까다롭다.

2. Github Actions이란 무엇인가?
    Github Actions이란 매번 github에 push할 때마다 black부터 pytest까지 모든 검사가 자동으로 실행되도록 만들어 주는 것이다.

3. Github Actions 동작법
    on: push = 액션이 언제 실행되는지를 정의합니다. on:push라면 push 할 때마다 실행을 합니다.
    jobs = 하나의 workflow는 여러개의 job으로 구성이 됩니다.
            2개의 job을 생성을 하면 2개로 쪼갠 이유는 실행 속도를 개선하기 위해서 입니다. 프로젝트가 점점 커지면
            가장 오래 걸리는 작업이 mypy 실행과 전체 단위 테스트 실행인데, 이 둘을 쪼개 병렬로 실행하면
            전체 UI가 더 빨리 끝납니다.
    runs-on = job이 실행되는 machine을 의미합니다.
    steps = 하나의 job은 여러개의 step으로 구성됩니다. step은 명령을 실행하거나다른 action을 실행합니다.
    uses = 실행할 action을 가리킵니다.
    with = action에 전달할 parameter변수입니다.
    run = 실행할 명령어 입니다.
    run: | = yaml문법입니다. |(파이프라인)을 사용해 value가 여러 줄(multiline)이라는 것을 의미합니다.

4. Github Actions에서 cache의 동작방식 대해
    워크플로 실행 간에 파일을 저장해두고 다음 실행에서 재사용해서 빌드/테스트 시간을 줄이는 기능입니다.

    기본 동작 흐름으로는
    1. 캐시 복원(restore)
        key를 기준으로 기존 캐시가 있는지 검색
        있으면 -> 캐시다운로드 & 지정 경로에 복원
        없으면 -> 그냥 새로 설치 및 빌드 진행
    2. 작업실행
        pip install 등 수행
        캐시 된 디렉터리가 있으면 이 단계가 빨라진다.
    3. 캐시저장
        해당 key가 이미 없을 때만 캐시 저장
        같은 key는 덮어쓰지 않는다.

5. spec API란 무엇인가?
    기능, 구조, 엔드포인트 등을 정의한 표준 명세 파일(예: OpenAPI Spec)을 의미하며 이를 통해 API문서화, 자동화, 품질 관리 등 다양한
    이점을 얻을 수 있다.

    주요 역할과 특징
    1. API문서화 = OpenAPI Spec은 API의 엔드포인트, 요청/응답 형식, 버전 정보등을 체계적으로 정리해 개발자와 시스템 모두 쉽게 이해 할 수
    있는 문서를 생성합니다.
    2. 자동화 및 테스트 = Swagger 등 도구를 활용해 Spec 파일을 기반으로 서버,클라이언트 코드를 자동 생성하거나, API테스트 환경을 구축할 수
    있습니다.
    3. 품질 및 유지보수 = 스펙을 먼저 작성하면 팀 간 일관성 확보, 기능 변경 시 문서의 신뢰성 유지, 코드 유지보수의 효율성이 높아집니다.

6. FastAPI의 내부에 구현된 모듈, 함수를 이용하여 라우터를 구성
