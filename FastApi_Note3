1. Open Api의 Swagger Api docs에 대해서 설명하시오.
    Swagger Api는 OpenAPI의 명세서를 바탕으로 문서를 시각화해주고, 실제로 API를 호출해서 볼 수 있게 해주는 도구입니다.

2. 써드 파티 라이브러리란?
    소프트웨어를 개발할 때 제작자 혹은 플랫폼 제작자가 아닌 제3자가 만든 코드 묶음을 의미합니다.
    예를 들어 FastApi, Pydantic, SQLAlchemy, Request 등이 있습니다.

3. 암호화, 복호화에 대해서 설명하세요.
    암호화 = 평문(누구나 읽을 수 있는 데이터)를 암호문(읽을 수 없는 외계어 같은 데이터)로 바꾸는 과정입니다
    복호화 = 암호문을 다시 원래의 평문으로 되돌리는 과정입니다.

4. sqids 파이썬 라이브러리에 대해서 설명하세요.
    sqids(스퀴즈)는 숫자를 짧고, 유니크하며, 해석하기 어려운 문자열로 인코딩해 주는 오픈소스 라이브러리입니다.

    특징
        1. 짧은ID생성: 큰 숫자(데이터베이스의 PK등)를 짧은 문자로 변환하여 URL을 깔끔하게 만듭니다.
        2. 복수 숫자인코딩: 하나가 아닌 여러개의 숫자를 하나의 문자열로 합칠 수 있습니다.
        3. 디코딩 기능: 암호화와 달리 규칙을 알면 다시 원래의 숫자로 되돌릴 수 있습니다.
        4. 욕설 필터링: 생성된 문자열에 무작위로 욕설이 포함되지 않도록 방지하는 기능이 내장되어 있습니다.
        5. 순서 보장 안 됨: 숫자가 1씩 증가해도 생성된 ID는 완전히 다른 모습이 되어, 외부인이 데이터의 양을 추측하기 어렵게 만듭니다.

5. FastAPI 내부에 구현된 모듈, 함수를 이용하여 SPEC API에 대한 라우터를 구성해보시오.
    1단계: 모듈 및 함수 정의
    FastApi 프로젝트 내에 필요한 모듈과 함수를 먼저 정의합니다. 예를 들어, api_spec.py에서 라우트, 엔드포인트,데이터모델을 모듈화 할 수 있습니다.

    2단계: 라우터 생성 및 등록
    FastAPI의 Router를 활용해 내부 모듈에서 정의한 엔드포인트를 라우터로 등록합니다.

    3단계: 인터페이스(SPEC)정의 및 통합
    FastAPI의 OpenAPI(RESTful) 인터페이스 자동 생성 기능을 활용해, 내부 모듈의 엔드포인트를 OpenAPI SPEC으로 자동 변활 할 수 있습니다.
    fastapi_openapi등 라이브러리를 사용하면, 라우터 등록과 동시에 자동 문서화가 가능합니다.